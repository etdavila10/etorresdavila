---
interface Props {
  authors: string[][];
}

const { authors } = Astro.props;
const totalAuthors = authors.length;
---

<ul>
  <li class="inline">In collaboration with:</li><!--
  -->{
    authors.map((author, i) => (
      <li class="inline">
      <a href={author[1]} class={ author[1] && ""}>{author[0]}</a><!--
        -->{i < totalAuthors - 2 && ","}<!--
        -->{i === totalAuthors - 1 && ""}<!--
        -->{i === totalAuthors - 2 && totalAuthors !== 2 && ", and"}<!--
        -->{i === totalAuthors - 2 && totalAuthors === 2 && " and"}
      </li>
    ))
  }<!--
-->
</ul>
