---
import headshot from '../images/profile-pic-web.jpeg'; 
import cvSnapshot from '../images/cv-snapshot.png'; 
---
<div id='home-section' class='flex flex-row flex-nowrap justify-center gap-24 min-h-screen items-center'>
  <div id='welcome-content-container' class='flex flex-col justify-start gap-12'>
    <h1 id='home-header' class='font-display first-letter:text-6xl text-5xl uppercase font-bold'>
      Welcome
    </h1>
    <div id='name-content-container'>
      <p>My name is</p>
      <h2 class='text-4xl font-display font-bold'>Eduardo Torres Dávila</h2>
    </div>
    <div id='identity-container'>
      <p>I am a</p>
      <h2 id='identity' class='select-none text-4xl font-display font-bold transition duration-500 -translate-x-12 opacity-0'>Graduate Student</h2>
    </div>
  </div>
  <div id='headshot-container' class='w-80 flex-none'>
    <img id='headshot' class='w-fit rounded-3xl border-gray-300 border' src={ headshot } alt="Eduardo's Headshot">
  </div>
</div>
<div id='about-me-container'>
  <h3 class='text-2xl mb-3 font-display underline'>About me</h3>
  <div id='about-cv-container' class='flex flex-row flex-nowrap justify-between gap-12'>
    <div id='about-me-content' class='text-justify flex-1'>
      <p class='pb-2'>
        I am currently a fourth year PhD student at the University of
        Minnesota. I graduated in May 2020 from San Diego State University with
        a Bachelors of Science in Applied Mathematics and a minor in Computer
        Science. Outside of academics, I enjoy the outdoors, programming, and climbing!
      </p>
      <p class='pb-2'>
        I am also a co-leader of the <a href='https://cause.umn.edu' class='transition duration-150 underline
          hover:text-sky-500'> Council for the Advancement of Underrepresented Scientists
          and Engineers (CAUSE)</a>. We are dedicated to the recruitment, retention, and
        professional development of graduate students of color and first generation college
        students within the College of Science and Engineering.
      </p>
      <p class='pb-2'>
        Want to learn more about me? Click <a href='/biography' class='underline transition duration-150 hover:text-sky-500'>here</a> to see my full bio.
      </p>
    </div>
    <a href='/etorresdavila_cv.pdf'>
      <div id='cv-card-container' class='group w-64 h-64 border border-gray-300 rounded-lg flex-none transition duration-500 hover:scale-105 hover:shadow-2xl'>
        <img id='cv-snapshot' class='overflow-hidden h-2/3 rounded-t-lg object-fill' src={ cvSnapshot } alt='cv preview'/>
        <div id='cv-card-details' class='h-1/3 border-t-2 border-gray-300 rounded-b-md p-2 bg-gray-200'>
          <h4 class='text-xl font-display transition duration-500 group-hover:text-sky-500'>Curriulum Vitae</h4>
          <p class='text-xs text-gray-400'>Last updated: August 13, 2023</p>
        </div>
      </div>
    </a>
  </div>
</div>

<script>
  const identifiers = ['Graduate Student', 'Mathematician', 'Researcher', 'Algebraist', 'Programmer',
    'Software Developer'];
  const numOfIdentifiers = identifiers.length;
  const identifierContainer = document.getElementById('identity-container')!;
  const identifier = document.getElementById('identity')!;
  let curIndex = 0;
  let initialLoad = true;

  const animateIdentifier = () => {
    if (initialLoad) {
      initialLoad = false
      identifier.classList.remove('-translate-x-12', 'opacity-0');
    }
    setInterval(() => {
      identifier.classList.add('opacity-0', 'translate-x-12');
      curIndex = (curIndex === numOfIdentifiers - 1) ? 0 : curIndex + 1;
      let curIdentifier = identifiers[curIndex];

      setTimeout(() => {
        identifier.innerHTML = curIdentifier;
        identifier.classList.remove('translate-x-12');
        identifier.classList.add('-translate-x-12');
      }, 500);

      setTimeout(() => {
        identifier.classList.remove('opacity-0', '-translate-x-12');
      }, 1000);
    }, 4000);
  };

  window.addEventListener("load",animateIdentifier,false);
</script>
